# GitHub personal access token used at build time to fetch contribution stats
# (GraphQL API) and per-repository commit counts for the /portfolio page.
# No extra scopes are required for public data.
GH_TOKEN=
